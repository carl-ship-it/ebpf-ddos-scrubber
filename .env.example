# DDoS Scrubber — Environment Configuration
# Copy to .env and customize.

# Version (auto-detected from git tags in CI)
VERSION=dev
BUILD_TIME=unknown

# Logging
LOG_LEVEL=info

# Frontend port
UI_PORT=8080

# VictoriaMetrics port
TSDB_PORT=8428

# Redis port
REDIS_PORT=6379

# Grafana (only with `docker compose --profile monitoring up`)
GRAFANA_PORT=3000
GRAFANA_USER=admin
GRAFANA_PASS=scrubber
